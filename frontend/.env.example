# Copy to .env.local for local dev overrides. Defaults to localhost:8000 if unset.
VITE_API_BASE_URL=http://localhost:8000

# The trip-route map on the Trip Details page uses Leaflet + OpenStreetMap
# tiles (via Nominatim/OSRM's public services) — no API key required.
